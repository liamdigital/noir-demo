<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>NOIR — Fine Dining Experience</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Heebo:wght@300;400;500;700;900&family=Space+Grotesk:wght@400;500;600;700&display=swap" rel="stylesheet">
<style>
/* ─── TOKENS ──────────────────────────────── */
:root {
  --black: #050403;
  --deep:  #0a0806;
  --void:  #020101;
  --gold:  #b8954a;
  --gold2: #d4af6e;
  --gold3: #eedfa8;
  --cream: #ede5d5;
  --muted: rgba(237,229,213,.44);
  --line:  rgba(184,149,74,.16);
  --glass: rgba(184,149,74,.05);
  /* TYPE */
  --f-display: 'Space Grotesk', 'Heebo', sans-serif;
  --f-body: 'Heebo', system-ui, sans-serif;
}
*{box-sizing:border-box;margin:0;padding:0}
html{
  background:var(--black);
  scroll-behavior:smooth;
  /* Lenis-style smooth scroll feel via CSS */
}
body{
  font-family: var(--f-body);
  direction:rtl;background:var(--black);color:var(--cream);
  overflow-x:hidden;
  -webkit-font-smoothing:antialiased;
  -moz-osx-font-smoothing:grayscale;
}
a{text-decoration:none;color:inherit}
::selection{background:rgba(184,149,74,.28);color:var(--cream)}

/* ─── SCROLL LOCK ───────────────────────── */
.locked{overflow:hidden}

/* ─── NOISE OVERLAY ─────────────────────── */
.noise{
  position:fixed;inset:0;z-index:999;pointer-events:none;
  opacity:.032;mix-blend-mode:overlay;
  background-image:url("data:image/svg+xml,%3Csvg viewBox='0 0 512 512' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.9' numOctaves='4'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
}

/* ─── CURSOR ────────────────────────────── */
.cursor{
  position:fixed;z-index:9999;pointer-events:none;
  width:10px;height:10px;border-radius:50%;
  background:var(--gold2);mix-blend-mode:difference;
  transform:translate(-50%,-50%);
  transition:width .25s,height .25s,opacity .25s;
}
.cursor-ring{
  position:fixed;z-index:9998;pointer-events:none;
  width:38px;height:38px;border-radius:50%;
  border:1px solid rgba(201,168,76,.5);
  transform:translate(-50%,-50%);
  transition:transform .12s cubic-bezier(.23,1,.32,1),width .3s,height .3s,opacity .25s;
}
body:has(a:hover) .cursor-ring,
body:has(button:hover) .cursor-ring{
  width:60px;height:60px;border-color:var(--gold2);
}

/* ─── NAV ───────────────────────────────── */
nav{
  position:fixed;top:0;right:0;left:0;z-index:500;
  height:72px;padding:0 5%;
  display:flex;align-items:center;justify-content:space-between;
  transition:background .6s cubic-bezier(.23,1,.32,1);
}
nav.scrolled{
  background:rgba(5,4,3,.88);
  backdrop-filter:blur(28px) saturate(1.4);
  -webkit-backdrop-filter:blur(28px) saturate(1.4);
  border-bottom:1px solid var(--line);
}
.brand{
  font-family: var(--f-display);
  font-size:22px;letter-spacing:8px;color:var(--cream);
  font-weight:600;text-transform:uppercase;
  display:flex;flex-direction:column;align-items:flex-start;gap:2px;
  line-height:1;
}
.brand-sub{
  font-family:var(--f-body);
  font-size:8px;letter-spacing:4px;color:var(--gold2);
  font-weight:400;text-transform:uppercase;
}
.navlinks{
  display:flex;gap:40px;color:var(--muted);
  font-family:var(--f-body);
  font-size:11px;font-weight:500;letter-spacing:2px;text-transform:uppercase;
}
.navlinks a{
  position:relative;transition:color .2s;
}
.navlinks a::after{
  content:"";position:absolute;bottom:-4px;right:0;left:0;
  height:1px;background:var(--gold);
  transform:scaleX(0);transform-origin:right;transition:.35s cubic-bezier(.23,1,.32,1);
}
.navlinks a:hover{color:var(--cream)}
.navlinks a:hover::after{transform:scaleX(1);transform-origin:left}
.navbtn{
  border:1px solid var(--line);padding:10px 22px;border-radius:1px;
  color:var(--cream);font-family:var(--f-body);
  font-size:10px;font-weight:600;letter-spacing:3px;
  text-transform:uppercase;background:transparent;
  transition:.25s cubic-bezier(.23,1,.32,1);cursor:pointer;
  position:relative;overflow:hidden;
}
.navbtn::before{
  content:"";position:absolute;inset:0;
  background:var(--gold);
  transform:translateX(105%);transition:.35s cubic-bezier(.23,1,.32,1);
  z-index:-1;
}
.navbtn:hover{color:var(--black);border-color:var(--gold)}
.navbtn:hover::before{transform:translateX(0)}

/* ─── HERO (Three.js canvas) ───────────── */
#hero{
  position:relative;width:100vw;height:100vh;
  overflow:hidden;
}
#three-canvas{
  position:absolute;inset:0;width:100%;height:100%;
}

/* hero copy — minimal, centered, maximum breathing room */
.hero-copy{
  position:absolute;inset:0;z-index:10;
  display:flex;flex-direction:column;
  align-items:center;justify-content:flex-end;
  text-align:center;
  pointer-events:none;
  padding-bottom:14vh;
}
.hero-eyebrow{
  font-family:var(--f-body);
  font-size:9px;letter-spacing:7px;text-transform:uppercase;
  color:var(--gold2);font-weight:500;
  margin-bottom:22px;opacity:0;
  animation:fadeUp .9s cubic-bezier(.23,1,.32,1) .5s forwards;
}
.hero-h1{
  font-family: var(--f-display);
  font-size:clamp(72px,13vw,200px);
  line-height:.86;letter-spacing:18px;font-weight:600;
  text-transform:uppercase;
  margin-bottom:18px;
  color:var(--cream);
  opacity:0;animation:fadeUp 1.2s cubic-bezier(.23,1,.32,1) .75s forwards;
  /* Subtle text shadow for depth over 3D */
  text-shadow:0 2px 60px rgba(0,0,0,.8), 0 0 120px rgba(0,0,0,.5);
}
.hero-tagline{
  font-family:var(--f-body);
  font-size:clamp(12px,1.1vw,16px);letter-spacing:5px;
  color:var(--muted);font-weight:400;text-transform:uppercase;
  margin-bottom:52px;
  opacity:0;animation:fadeUp .9s cubic-bezier(.23,1,.32,1) 1.1s forwards;
}
.hero-actions{
  display:flex;gap:12px;pointer-events:all;
  opacity:0;animation:fadeUp .9s cubic-bezier(.23,1,.32,1) 1.35s forwards;
}

/* VIGNETTE */
#hero::after{
  content:"";position:absolute;inset:0;z-index:5;pointer-events:none;
  background:
    radial-gradient(ellipse 70% 55% at 50% 50%, transparent 30%, rgba(5,4,3,.65) 100%),
    linear-gradient(to top, rgba(5,4,3,.9) 0%, transparent 35%),
    linear-gradient(to bottom, rgba(5,4,3,.6) 0%, transparent 25%);
}

/* SCROLL INDICATOR */
.scroll-cue{
  position:absolute;bottom:36px;left:50%;transform:translateX(-50%);
  z-index:10;display:flex;flex-direction:column;align-items:center;gap:10px;
  color:var(--muted);
  font-family:var(--f-body);
  font-size:9px;letter-spacing:5px;text-transform:uppercase;
  opacity:0;animation:fadeUp .9s cubic-bezier(.23,1,.32,1) 1.8s forwards;
}
.scroll-line{
  width:1px;height:50px;background:linear-gradient(to bottom,var(--gold2),transparent);
  animation:scrollPulse 2.2s ease-in-out infinite;
}
@keyframes scrollPulse{
  0%,100%{opacity:1;transform:scaleY(1)}
  50%{opacity:.25;transform:scaleY(.35)}
}
/* SPOTLIGHT that follows mouse */
.hero-spot{
  position:absolute;inset:0;z-index:4;pointer-events:none;
  background:radial-gradient(circle 420px at 50% 50%,rgba(184,149,74,.06),transparent 65%);
  transition:background .06s;
}

/* ─── BTN STYLES ─────────────────────────── */
.btn{
  display:inline-flex;align-items:center;gap:10px;
  padding:15px 32px;border-radius:1px;
  font-family:var(--f-body);
  font-size:10px;font-weight:600;letter-spacing:3px;text-transform:uppercase;
  transition:.4s cubic-bezier(.23,1,.32,1);cursor:pointer;border:0;
  position:relative;overflow:hidden;
}
.btn-primary{
  background:var(--gold);color:var(--black);
  box-shadow:0 16px 50px rgba(184,149,74,.22);
}
.btn-primary::after{
  content:"";position:absolute;inset:0;
  background:linear-gradient(to right,transparent,rgba(255,255,255,.15),transparent);
  transform:translateX(-100%);transition:.6s cubic-bezier(.23,1,.32,1);
}
.btn-primary:hover::after{transform:translateX(100%)}
.btn-primary:hover{
  background:var(--gold3);color:var(--black);
  box-shadow:0 24px 80px rgba(184,149,74,.38);
  transform:translateY(-4px);
}
.btn-ghost{
  border:1px solid rgba(237,229,213,.22);color:var(--cream);background:transparent;
}
.btn-ghost:hover{
  border-color:var(--gold2);color:var(--gold2);
  transform:translateY(-4px);
  background:rgba(184,149,74,.06);
  box-shadow:0 0 0 1px var(--gold2) inset;
}

/* ─── SECTION BASE ───────────────────────── */
section{position:relative;z-index:2}

/* ─── MANIFESTO STRIP ────────────────────── */
#manifesto{
  padding:120px 6% 120px;
  display:grid;grid-template-columns:.85fr 2fr;gap:60px;
  align-items:end;
  /* Pull up to overlap hero bottom edge */
  margin-top:-120px;
  position:relative;z-index:10;
  background:linear-gradient(to bottom,transparent 0%,var(--black) 18%);
  overflow:hidden;
}
.manifesto-num{
  font-family:var(--f-display);
  font-size:clamp(100px,16vw,240px);
  line-height:1;letter-spacing:-4px;font-weight:700;
  color:transparent;
  -webkit-text-stroke:1px rgba(184,149,74,.16);
  pointer-events:none;user-select:none;
  padding-top:60px;
}
.manifesto-text h2{
  font-family:var(--f-display);
  font-size:clamp(34px,4.2vw,68px);line-height:1.0;
  letter-spacing:-1.5px;font-weight:600;margin-bottom:28px;
  color:var(--cream);
}
.manifesto-text p{
  font-family:var(--f-body);
  color:var(--muted);font-size:16px;line-height:2.1;
  max-width:520px;font-weight:400;
}
.manifesto-line{
  width:100%;height:1px;
  background:linear-gradient(to right,var(--gold),transparent);
  margin-bottom:44px;opacity:.3;
}

/* ─── MENU THEATRE ───────────────────────── */
#menu{
  padding:0;overflow:hidden;
  position:relative;
}
/* Diagonal rule across the section */
#menu::before{
  content:"";position:absolute;top:0;right:0;left:0;height:1px;
  background:linear-gradient(to right, transparent 0%, var(--gold) 30%, transparent 100%);
  opacity:.25;z-index:2;
}
.menu-header{
  padding:100px 6% 56px;
  display:flex;justify-content:space-between;align-items:flex-end;
  border-bottom:1px solid var(--line);
}
.menu-header h2{
  font-family:var(--f-display);
  font-size:clamp(48px,7.5vw,115px);line-height:.84;
  letter-spacing:-3px;font-weight:700;color:var(--cream);
}
.menu-header h2 em{
  font-style:normal;color:var(--gold2);
  display:block;font-weight:500;letter-spacing:0;
}
.menu-header p{
  font-family:var(--f-body);
  color:var(--muted);font-size:14px;max-width:280px;line-height:1.9;
  text-align:left;font-weight:400;
}

/* horizontal scroll track */
.menu-track-wrap{
  overflow-x:auto;overflow-y:visible;
  scroll-snap-type:x mandatory;
  -webkit-overflow-scrolling:touch;
  scrollbar-width:none;cursor:grab;
  padding:56px 6% 56px;
  display:flex;gap:16px;
}
.menu-track-wrap::-webkit-scrollbar{display:none}
.menu-track-wrap:active{cursor:grabbing}

.dish-card{
  flex:0 0 320px;scroll-snap-align:start;
  border-radius:0;overflow:hidden;
  border:1px solid rgba(184,149,74,.12);
  background:var(--deep);
  transition:.45s cubic-bezier(.23,1,.32,1);
  position:relative;
}
/* Glass shimmer on hover */
.dish-card::before{
  content:"";position:absolute;inset:0;z-index:2;pointer-events:none;
  background:linear-gradient(135deg,rgba(255,255,255,.04) 0%,transparent 50%,rgba(255,255,255,.02) 100%);
  opacity:0;transition:.45s;
}
.dish-card:hover::before{opacity:1}
.dish-card:hover{
  transform:translateY(-16px);
  box-shadow:0 60px 100px rgba(0,0,0,.6),0 0 0 1px rgba(184,149,74,.28);
  border-color:rgba(184,149,74,.28);
}
.dish-photo{
  height:270px;background-size:cover;background-position:center;
  position:relative;overflow:hidden;
}
.dish-photo img{
  width:100%;height:100%;object-fit:cover;display:block;
  transition:transform .8s cubic-bezier(.23,1,.32,1),filter .6s;
  filter:brightness(.78) saturate(.8);
}
.dish-card:hover .dish-photo img{transform:scale(1.06);filter:brightness(.9) saturate(1)}
.dish-photo::after{
  content:"";position:absolute;inset:0;
  background:linear-gradient(to top,var(--deep) 0%,transparent 50%);
}
.dish-photo .price-tag{
  position:absolute;top:20px;left:20px;z-index:2;
  font-family:var(--f-display);
  font-size:22px;font-weight:600;color:var(--gold2);
  letter-spacing:1px;
}
.dish-info{padding:24px 26px 30px}
.dish-cat{
  font-family:var(--f-body);
  font-size:9px;letter-spacing:4px;text-transform:uppercase;
  color:var(--gold);font-weight:500;margin-bottom:10px;
}
.dish-info h3{
  font-family:var(--f-display);
  font-size:26px;font-weight:600;margin-bottom:10px;line-height:1.1;
  color:var(--cream);letter-spacing:-.5px;
}
.dish-info p{
  font-family:var(--f-body);
  color:var(--muted);font-size:13px;line-height:1.75;font-weight:400;
}

.drag-hint{
  padding:0 6% 70px;
  display:flex;align-items:center;gap:16px;
  font-family:var(--f-body);
  color:var(--muted);font-size:9px;letter-spacing:4px;text-transform:uppercase;
  font-weight:500;
}
.drag-hint::before{
  content:"";display:inline-block;width:36px;height:1px;background:var(--gold);opacity:.4;
}

/* ─── CHEF STORY ─────────────────────────── */
#story{
  padding:140px 6%;
  display:grid;grid-template-columns:1.15fr .85fr;gap:80px;
  align-items:center;
  border-top:1px solid var(--line);
}
.story-visual{
  position:relative;height:680px;
}
.story-img-back{
  position:absolute;top:0;right:0;width:88%;height:78%;
  overflow:hidden;
  border:1px solid rgba(184,149,74,.12);
}
.story-img-back img{width:100%;height:100%;object-fit:cover;filter:brightness(.65) saturate(.7);transition:.8s}
.story-img-back:hover img{filter:brightness(.8) saturate(.9)}
.story-img-front{
  position:absolute;bottom:0;left:0;width:58%;height:52%;
  overflow:hidden;
  border:1px solid rgba(184,149,74,.3);
  box-shadow:0 40px 100px rgba(0,0,0,.75);
}
.story-img-front img{width:100%;height:100%;object-fit:cover}
.story-badge{
  position:absolute;bottom:44px;right:-24px;
  width:104px;height:104px;border-radius:50%;
  border:1px solid var(--line);background:var(--deep);
  display:flex;flex-direction:column;align-items:center;justify-content:center;
  font-family:var(--f-display);
  animation:spinSlow 22s linear infinite;
}
.story-badge span:first-child{
  font-size:32px;color:var(--gold2);font-weight:700;line-height:1;
}
.story-badge span:last-child{
  font-family:var(--f-body);
  font-size:8px;color:var(--muted);letter-spacing:2px;text-transform:uppercase;margin-top:2px;
}
@keyframes spinSlow{to{transform:rotate(360deg)}}
.story-text{padding-right:10px}
.story-text .eyebrow{
  font-family:var(--f-body);
  font-size:9px;letter-spacing:5px;color:var(--gold);font-weight:500;
  text-transform:uppercase;margin-bottom:28px;display:block;
}
.story-text h2{
  font-family:var(--f-display);
  font-size:clamp(40px,5vw,76px);line-height:.92;
  letter-spacing:-2px;font-weight:700;margin-bottom:36px;
  color:var(--cream);
}
.story-text h2 em{font-style:normal;color:var(--gold2)}
.story-text p{
  font-family:var(--f-body);
  color:var(--muted);font-size:15px;line-height:2.1;margin-bottom:18px;font-weight:400;
}
.story-sig{
  margin-top:44px;display:flex;align-items:center;gap:20px;
  padding-top:32px;border-top:1px solid var(--line);
}
.sig-line{width:44px;height:1px;background:var(--gold);opacity:.4;flex-shrink:0}
.sig-text{
  font-family:var(--f-display);
  font-size:20px;color:var(--gold3);font-weight:500;letter-spacing:1px;
}

/* ─── GALLERY — ASYMMETRIC ───────────────── */
#gallery{
  padding:120px 6%;
  border-top:1px solid var(--line);
}
.gallery-eyebrow{
  display:flex;align-items:center;gap:20px;margin-bottom:56px;
}
.gallery-eyebrow span{
  font-family:var(--f-body);
  font-size:9px;letter-spacing:5px;text-transform:uppercase;color:var(--gold);font-weight:500;
}
.gallery-eyebrow::after{content:"";flex:1;height:1px;background:var(--line)}
.gallery-masonry{
  display:grid;
  grid-template-columns:repeat(12,1fr);
  grid-template-rows:auto;
  gap:10px;
}
.gshot{
  overflow:hidden;
  position:relative;border:1px solid rgba(184,149,74,.08);
  background:var(--deep);cursor:pointer;
  /* Parallax container */
}
.gshot img{
  width:100%;height:100%;object-fit:cover;display:block;
  transition:transform .9s cubic-bezier(.23,1,.32,1),filter .6s;
  filter:brightness(.72) saturate(.65) contrast(1.05);
  transform-origin:center center;
}
.gshot:hover img{transform:scale(1.07);filter:brightness(.92) saturate(1) contrast(1)}
.gshot-overlay{
  position:absolute;inset:0;
  background:linear-gradient(to top,rgba(5,4,3,.82) 0%,transparent 55%);
  opacity:0;transition:.4s cubic-bezier(.23,1,.32,1);
}
.gshot:hover .gshot-overlay{opacity:1}
.gshot-label{
  position:absolute;bottom:20px;right:20px;
  font-family:var(--f-display);
  font-size:18px;color:var(--cream);font-weight:600;
  letter-spacing:.5px;
  opacity:0;transform:translateY(12px);transition:.4s .06s cubic-bezier(.23,1,.32,1);
}
.gshot-num{
  position:absolute;top:18px;left:18px;
  font-family:var(--f-body);
  font-size:9px;color:var(--gold);letter-spacing:3px;text-transform:uppercase;
  opacity:0;transition:.35s;
}
.gshot:hover .gshot-label{opacity:1;transform:translateY(0)}
.gshot:hover .gshot-num{opacity:1}
/* grid placement */
.g1{grid-column:span 7;min-height:440px}
.g2{grid-column:span 5;min-height:440px}
.g3{grid-column:span 4;min-height:300px}
.g4{grid-column:span 4;min-height:300px}
.g5{grid-column:span 4;min-height:300px}

/* ─── RESERVATION ────────────────────────── */
#reserve{
  padding:140px 6%;
  display:grid;grid-template-columns:1fr 1.1fr;gap:60px;
  align-items:start;
  border-top:1px solid var(--line);
  position:relative;overflow:hidden;
}
#reserve::before{
  content:"";position:absolute;
  top:-20%;left:-10%;width:60%;height:80%;
  background:radial-gradient(circle,rgba(184,149,74,.04),transparent 65%);
  pointer-events:none;
}
.reserve-left h2{
  font-family:var(--f-display);
  font-size:clamp(48px,6.5vw,100px);line-height:.86;
  letter-spacing:-3px;font-weight:700;margin-bottom:28px;color:var(--cream);
}
.reserve-left h2 em{font-style:normal;color:var(--gold2)}
.reserve-left p{
  font-family:var(--f-body);
  color:var(--muted);font-size:15px;line-height:2.1;max-width:400px;margin-bottom:50px;font-weight:400;
}
.reserve-detail{
  display:flex;flex-direction:column;gap:0;
  border-top:1px solid var(--line);
}
.rd-item{
  display:flex;align-items:flex-start;gap:22px;
  padding:24px 0;border-bottom:1px solid var(--line);
  transition:.3s;
}
.rd-item:hover{padding-right:6px}
.rd-num{
  font-family:var(--f-display);
  font-size:38px;font-weight:700;color:var(--gold);
  line-height:1;min-width:66px;
}
.rd-text strong{
  font-family:var(--f-display);
  display:block;font-size:14px;font-weight:600;letter-spacing:.5px;margin-bottom:4px;color:var(--cream);
}
.rd-text span{
  font-family:var(--f-body);
  color:var(--muted);font-size:12px;font-weight:400;letter-spacing:.5px;
}
.form-panel{
  background:rgba(10,8,6,.75);
  border:1px solid var(--line);
  padding:48px;
  backdrop-filter:blur(24px);
  -webkit-backdrop-filter:blur(24px);
  position:sticky;top:100px;
}
.form-panel .fp-title{
  font-family:var(--f-display);
  font-size:32px;font-weight:700;margin-bottom:36px;color:var(--cream);
  letter-spacing:-.5px;
  padding-bottom:28px;border-bottom:1px solid var(--line);
}
.field-group{display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-bottom:14px}
.field-group.full{grid-template-columns:1fr}
.field{display:flex;flex-direction:column;gap:8px}
.field label{
  font-family:var(--f-body);
  font-size:8px;letter-spacing:4px;text-transform:uppercase;
  color:var(--gold);font-weight:600;
}
.field input,
.field select,
.field textarea{
  background:rgba(255,255,255,.035);
  border:1px solid rgba(184,149,74,.16);
  padding:14px 16px;
  color:var(--cream);
  font-family:var(--f-body);
  font-size:14px;font-weight:400;
  outline:none;transition:.3s;
  -webkit-appearance:none;
  border-radius:0;
}
.field input:focus,
.field select:focus,
.field textarea:focus{
  border-color:var(--gold);
  background:rgba(184,149,74,.04);
  box-shadow:0 0 0 3px rgba(184,149,74,.06);
}
.field input::placeholder{color:rgba(237,229,213,.22);font-size:13px}
.field textarea{min-height:96px;resize:none}
.submit-btn{
  width:100%;margin-top:22px;
  background:var(--gold);color:var(--black);
  border:0;padding:17px;
  font-family:var(--f-body);
  font-size:10px;font-weight:700;letter-spacing:4px;
  text-transform:uppercase;cursor:pointer;
  transition:.4s cubic-bezier(.23,1,.32,1);
  position:relative;overflow:hidden;
  border-radius:0;
}
.submit-btn::after{
  content:"";position:absolute;inset:0;
  background:linear-gradient(to right,transparent,rgba(255,255,255,.18),transparent);
  transform:translateX(-100%);transition:.6s cubic-bezier(.23,1,.32,1);
}
.submit-btn:hover::after{transform:translateX(100%)}
.submit-btn:hover{background:var(--gold3);transform:translateY(-2px);box-shadow:0 20px 60px rgba(184,149,74,.32)}

/* ─── FOOTER ─────────────────────────────── */
footer{
  padding:54px 6%;
  border-top:1px solid var(--line);
  display:grid;grid-template-columns:1fr auto 1fr;
  align-items:center;gap:30px;
}
.footer-brand{
  font-family:var(--f-display);
  font-size:20px;letter-spacing:7px;color:var(--cream);font-weight:600;
  text-transform:uppercase;
}
.footer-center{
  text-align:center;
  font-family:var(--f-body);
  color:var(--muted);font-size:9px;letter-spacing:3px;
  text-transform:uppercase;line-height:2.2;font-weight:400;
}
.footer-right{
  text-align:left;
  font-family:var(--f-body);
  color:var(--muted);font-size:9px;letter-spacing:2px;line-height:2;
}
.footer-links{display:flex;gap:24px;justify-content:flex-end}
.footer-links a{
  font-family:var(--f-body);
  color:var(--muted);font-size:9px;letter-spacing:2px;text-transform:uppercase;
  transition:.2s;font-weight:500;
}
.footer-links a:hover{color:var(--gold2)}

/* ─── REVEAL ANIMATION ───────────────────── */
.reveal{
  opacity:0;transform:translateY(36px);
  transition:opacity 1s cubic-bezier(.23,1,.32,1),transform 1s cubic-bezier(.23,1,.32,1);
}
.reveal.visible{opacity:1;transform:translateY(0)}
.reveal-delay-1{transition-delay:.12s}
.reveal-delay-2{transition-delay:.24s}
.reveal-delay-3{transition-delay:.4s}

/* Text split reveal */
.split-reveal .word{
  overflow:hidden;display:inline-block;
}
.split-reveal .word .inner{
  display:inline-block;
  transform:translateY(110%);
  transition:transform 1s cubic-bezier(.23,1,.32,1);
}
.split-reveal.visible .word .inner{transform:translateY(0)}

@keyframes fadeUp{
  from{opacity:0;transform:translateY(24px)}
  to{opacity:1;transform:translateY(0)}
}

/* ─── MOBILE ─────────────────────────────── */
@media(max-width:900px){
  .navlinks{display:none}
  #manifesto{grid-template-columns:1fr;gap:0;margin-top:-60px}
  .manifesto-num{font-size:clamp(80px,22vw,160px)}
  #story{grid-template-columns:1fr;padding:80px 5%}
  .story-visual{height:460px;margin-bottom:60px}
  .story-badge{right:10px}
  .gallery-masonry{grid-template-columns:repeat(6,1fr)}
  .g1,.g2{grid-column:span 6;min-height:300px}
  .g3,.g4,.g5{grid-column:span 6;min-height:240px}
  #reserve{grid-template-columns:1fr;padding:80px 5%}
  .form-panel{position:static}
  footer{grid-template-columns:1fr;text-align:center;gap:16px}
  .footer-links{justify-content:center}
  .menu-header{flex-direction:column;align-items:flex-start;gap:24px}
  .hero-h1{letter-spacing:8px}
}

/* ─── LIAM POLISH PASS ─────────────────────
   פונט מודרני יותר, לוגו באנגלית, היררכיה נקייה יותר
*/
.brand{
  align-items:flex-start;
  gap:4px;
}
.brand-sub{
  display:block;
  margin-top:3px;
}
.hero-h1{
  direction:ltr;
  letter-spacing:clamp(8px,1.45vw,24px);
  font-weight:700;
}
.hero-tagline{
  font-family:var(--f-display);
  direction:ltr;
  font-size:clamp(15px,1.6vw,24px);
  letter-spacing:clamp(3px,.55vw,9px);
  text-transform:uppercase;
  color:var(--gold2);
  margin-bottom:16px;
  opacity:0;
  animation:fadeUp .9s cubic-bezier(.23,1,.32,1) 1s forwards;
}
.hero-sub{
  font-family:var(--f-body);
  font-size:clamp(14px,1.2vw,17px);
  letter-spacing:.3px;
  color:rgba(237,229,213,.62);
  margin-bottom:42px;
}
.menu-header h2 em,
.reserve-left h2 em{
  font-family:var(--f-display)!important;
  font-style:normal!important;
}
.story-text h2,
.manifesto-text h2,
.menu-header h2,
.reserve-left h2{
  letter-spacing:-1px;
}
.dish-info h3,
.fp-title,
.rd-text strong{
  letter-spacing:0;
}
.footer-brand{
  direction:ltr;
}
@media(max-width:900px){
  .hero-h1{
    letter-spacing:8px;
  }
}

</style>
</head>
<body>

<!-- CURSOR -->
<div class="cursor" id="cursor"></div>
<div class="cursor-ring" id="cursor-ring"></div>

<!-- NOISE -->
<div class="noise"></div>

<!-- ═══════════════════════ NAV ═══════════════════════ -->
<nav id="nav">
  <a class="brand" href="#hero">NOIR<span class="brand-sub">Fine Dining</span></a>
  <div class="navlinks">
    <a href="#menu">תפריט</a>
    <a href="#story">השף</a>
    <a href="#gallery">גלריה</a>
    <a href="#reserve">הזמנה</a>
  </div>
  <a class="navbtn btn" href="#reserve">הזמנת שולחן</a>
</nav>

<!-- ═══════════════════════ HERO ══════════════════════ -->
<section id="hero">
  <canvas id="three-canvas"></canvas>
  <div class="hero-spot" id="hero-spot"></div>

  <div class="hero-copy">
    <div class="hero-eyebrow">Fine Dining · Tel Aviv · 2025</div>
    <h1 class="hero-h1">NOIR</h1>
    <p class="hero-tagline">Dining Beyond Taste</p>
    <p class="hero-sub">חוויה קולינרית רב־חושית בתל אביב</p>
    <div class="hero-actions">
      <a class="btn btn-primary" href="#reserve">הזמנת שולחן</a>
      <a class="btn btn-ghost" href="#menu">תפריט ערב</a>
    </div>
  </div>

  <div class="scroll-cue">
    <div class="scroll-line"></div>
    <span>גללו</span>
  </div>
</section>

<!-- ═══════════════════════ MANIFESTO ════════════════ -->
<section id="manifesto">
  <div class="manifesto-num reveal">01</div>
  <div class="manifesto-text">
    <div class="manifesto-line"></div>
    <h2 class="reveal reveal-delay-1">
      מסעדה שבנויה<br>כמו סצנה קולנועית
    </h2>
    <p class="reveal reveal-delay-2">
      כל פרט — האור מעל השולחן, רעש הצלחות, ריח היין — נבחר בקפידה. 
      נואר היא לא ארוחה. היא חוויה שמתחילה שבועות לפני שאתה מגיע, 
      ונגמרת שבועות אחרי שיצאת.
    </p>
  </div>
</section>

<!-- ═══════════════════════ MENU THEATRE ════════════ -->
<section id="menu">
  <div class="menu-header">
    <h2 class="reveal">תפריט<br><em style="font-style:italic;color:var(--gold2);font-family:var(--f-display)">ערב</em></h2>
    <p class="reveal reveal-delay-1">שבע מנות. ארבעה קוקטיילים. יין מותאם אישית. שולחן שף.</p>
  </div>

  <div class="menu-track-wrap" id="menuTrack">
    <article class="dish-card">
      <div class="dish-photo" style="background-image:url('https://images.unsplash.com/photo-1544025162-d76694265947?auto=format&fit=crop&w=700&q=90')">
        <span class="price-tag">₪78</span>
      </div>
      <div class="dish-info">
        <div class="dish-cat">פתיח · קרודו</div>
        <h3>המאצ׳י יוזו</h3>
        <p>שמן בזיליקום, מלח ים שחור, צ׳ילי עדין וציפור׳ס.</p>
      </div>
    </article>

    <article class="dish-card">
      <div class="dish-photo" style="background-image:url('https://images.unsplash.com/photo-1546549032-9571cd6b27df?auto=format&fit=crop&w=700&q=90')">
        <span class="price-tag">₪64</span>
      </div>
      <div class="dish-info">
        <div class="dish-cat">ביניים · בריוש</div>
        <h3>כמהין ודבש מעושן</h3>
        <p>בריוש חם, חמאת כמהין שחורה, פרמזן 36 חודש.</p>
      </div>
    </article>

    <article class="dish-card">
      <div class="dish-photo" style="background-image:url('https://images.unsplash.com/photo-1559847844-5315695dadae?auto=format&fit=crop&w=700&q=90')">
        <span class="price-tag">₪310</span>
      </div>
      <div class="dish-info">
        <div class="dish-cat">עיקרית · בקר</div>
        <h3>סטריפלוין וואגיו A5</h3>
        <p>צרוב על פחם עץ אלון, שום שחור, רוטב יין ישן.</p>
      </div>
    </article>

    <article class="dish-card">
      <div class="dish-photo" style="background-image:url('https://images.unsplash.com/photo-1559339352-11d035aa65de?auto=format&fit=crop&w=700&q=90')">
        <span class="price-tag">₪186</span>
      </div>
      <div class="dish-info">
        <div class="dish-cat">עיקרית · ים</div>
        <h3>קוד שחור מיסו</h3>
        <p>קרם תירס צלוי, שמן עשבי בר, פירורי נורי.</p>
      </div>
    </article>

    <article class="dish-card">
      <div class="dish-photo" style="background-image:url('https://images.unsplash.com/photo-1606313564200-e75d5e30476c?auto=format&fit=crop&w=700&q=90')">
        <span class="price-tag">₪66</span>
      </div>
      <div class="dish-info">
        <div class="dish-cat">קינוח · שוקולד</div>
        <h3>כדור נמס</h3>
        <p>שוקולד 85%, קרמל מלוח, גלידת וניל בורבון.</p>
      </div>
    </article>

    <article class="dish-card">
      <div class="dish-photo" style="background-image:url('https://images.unsplash.com/photo-1470337458703-46ad1756a187?auto=format&fit=crop&w=700&q=90')">
        <span class="price-tag">₪74</span>
      </div>
      <div class="dish-info">
        <div class="dish-cat">קוקטייל · חתימה</div>
        <h3>עשן נואר</h3>
        <p>וויסקי 12 שנה, דובדבן שחור, עשן עץ אלון.</p>
      </div>
    </article>

    <article class="dish-card">
      <div class="dish-photo" style="background-image:url('https://images.unsplash.com/photo-1414235077428-338989a2e8c0?auto=format&fit=crop&w=700&q=90')">
        <span class="price-tag">₪420</span>
      </div>
      <div class="dish-info">
        <div class="dish-cat">תפריט שלם · שף</div>
        <h3>תפריט טעימות</h3>
        <p>7 מנות + 4 קוקטיילים + יין מותאם. 2.5 שעות.</p>
      </div>
    </article>
  </div>
  <div class="drag-hint reveal">גררו לצפייה בתפריט המלא</div>
</section>

<!-- ═══════════════════════ CHEF STORY ═══════════════ -->
<section id="story">
  <div class="story-visual reveal">
    <div class="story-img-back">
      <img src="https://images.unsplash.com/photo-1414235077428-338989a2e8c0?auto=format&fit=crop&w=1200&q=90" alt="מסעדה">
    </div>
    <div class="story-img-front">
      <img src="https://images.unsplash.com/photo-1551218808-94e220e084d2?auto=format&fit=crop&w=800&q=90" alt="שף">
    </div>
    <div class="story-badge">
      <span>18</span>
      <span>שנות נסיון</span>
    </div>
  </div>

  <div class="story-text">
    <span class="eyebrow reveal">· הסיפור שמאחורי הצלחת</span>
    <h2 class="reveal reveal-delay-1">
      בישול<br>
      <em>הוא שפה</em><br>
      שדוברת<br>
      בשתיקה
    </h2>
    <p class="reveal reveal-delay-2">
      לפני שמגיעים לשולחן, יש שנים של מחקר בכל פינה של העולם — 
      מרחובות טוקיו ועד מטבחי פריז.
    </p>
    <p class="reveal reveal-delay-3">
      כל מנה מספרת פרק. כל טעם הוא זיכרון שעוד לא היה לך.
    </p>
    <div class="story-sig reveal">
      <div class="sig-line"></div>
      <div class="sig-text">השף נואר, 2025</div>
    </div>
  </div>
</section>

<!-- ═══════════════════════ GALLERY ══════════════════ -->
<section id="gallery">
  <div class="gallery-eyebrow reveal">
    <span>גלריה · האווירה</span>
  </div>
  <div class="gallery-masonry">
    <div class="gshot g1 reveal">
      <img src="https://images.unsplash.com/photo-1514933651103-005eec06c04b?auto=format&fit=crop&w=1400&q=90" alt="מסעדה">
      <div class="gshot-overlay"></div>
      <div class="gshot-label">האולם המרכזי</div>
    </div>
    <div class="gshot g2 reveal reveal-delay-1">
      <img src="https://images.unsplash.com/photo-1559339352-11d035aa65de?auto=format&fit=crop&w=900&q=90" alt="קוקטייל">
      <div class="gshot-overlay"></div>
      <div class="gshot-label">בר נואר</div>
    </div>
    <div class="gshot g3 reveal">
      <img src="https://images.unsplash.com/photo-1470337458703-46ad1756a187?auto=format&fit=crop&w=700&q=90" alt="בר">
      <div class="gshot-overlay"></div>
      <div class="gshot-label">הבר</div>
    </div>
    <div class="gshot g4 reveal reveal-delay-1">
      <img src="https://images.unsplash.com/photo-1544025162-d76694265947?auto=format&fit=crop&w=700&q=90" alt="מנה">
      <div class="gshot-overlay"></div>
      <div class="gshot-label">קרפצ׳יו</div>
    </div>
    <div class="gshot g5 reveal reveal-delay-2">
      <img src="https://images.unsplash.com/photo-1551218808-94e220e084d2?auto=format&fit=crop&w=700&q=90" alt="שף">
      <div class="gshot-overlay"></div>
      <div class="gshot-label">השף</div>
    </div>
  </div>
</section>

<!-- ═══════════════════════ RESERVATION ══════════════ -->
<section id="reserve">
  <div class="reserve-left">
    <h2 class="reveal">הזמינו<br><em style="font-style:italic;color:var(--gold2);font-family:var(--f-display)">שולחן</em></h2>
    <p class="reveal reveal-delay-1">
      שולחן שף מוגבל ל-8 אורחים בלילה. כל ארוחה היא הצגה בלעדית.
    </p>
    <div class="reserve-detail">
      <div class="rd-item reveal">
        <div class="rd-num">19</div>
        <div class="rd-text">
          <strong>פתיחת ערב</strong>
          <span>שולחן שף · תפריט טעימות מלא</span>
        </div>
      </div>
      <div class="rd-item reveal reveal-delay-1">
        <div class="rd-num">8</div>
        <div class="rd-text">
          <strong>אורחים מקסימום</strong>
          <span>חוויה אינטימית ומוגבלת</span>
        </div>
      </div>
      <div class="rd-item reveal reveal-delay-2">
        <div class="rd-num">2.5</div>
        <div class="rd-text">
          <strong>שעות של חוויה</strong>
          <span>מהקוקטייל הראשון עד הקינוח האחרון</span>
        </div>
      </div>
    </div>
  </div>

  <div class="form-panel reveal reveal-delay-1">
    <div class="fp-title">הזמנת שולחן</div>
    <div class="field-group">
      <div class="field">
        <label>שם מלא</label>
        <input type="text" placeholder="לדוגמה: דניאל כהן">
      </div>
      <div class="field">
        <label>טלפון</label>
        <input type="tel" placeholder="050-0000000">
      </div>
    </div>
    <div class="field-group">
      <div class="field">
        <label>תאריך</label>
        <input type="date">
      </div>
      <div class="field">
        <label>שעה</label>
        <select>
          <option>19:00 — ערב ראשון</option>
          <option>20:30 — ערב שני</option>
          <option>22:00 — ערב מאוחר</option>
        </select>
      </div>
    </div>
    <div class="field-group">
      <div class="field">
        <label>חוויה</label>
        <select>
          <option>שולחן שף — תפריט טעימות</option>
          <option>חדר פרטי — 18 אורחים</option>
          <option>התאמת יין — Sommelier</option>
        </select>
      </div>
      <div class="field">
        <label>אורחים</label>
        <select>
          <option>2 אורחים</option>
          <option>4 אורחים</option>
          <option>6 אורחים</option>
          <option>אירוע פרטי</option>
        </select>
      </div>
    </div>
    <div class="field-group full">
      <div class="field">
        <label>בקשה מיוחדת</label>
        <textarea placeholder="יום הולדת, אלרגיות, אירוע פרטי, חדר נפרד..."></textarea>
      </div>
    </div>
    <button class="submit-btn" type="button">אישור הזמנה →</button>
  </div>
</section>

<!-- ═══════════════════════ FOOTER ═══════════════════ -->
<footer>
  <div class="footer-brand">NOIR</div>
  <div class="footer-center">
    תל אביב · שעות פעילות ב–ש, 19:00–24:00<br>
    © 2025 NOIR Restaurant · כל הזכויות שמורות
  </div>
  <div class="footer-right">
    <div class="footer-links">
      <a href="#menu">תפריט</a>
      <a href="#story">השף</a>
      <a href="#reserve">הזמנה</a>
    </div>
  </div>
</footer>

<!-- ═════════════════════ THREE.JS ══════════════════ -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
<script>
/* ══════════════════════════════════════════════
   THREE.JS — Immersive 3D Restaurant Scene
   ══════════════════════════════════════════════ */
(function() {
  const canvas = document.getElementById('three-canvas');
  const W = () => window.innerWidth;
  const H = () => window.innerHeight;

  // Renderer
  const renderer = new THREE.WebGLRenderer({ canvas, antialias: true, alpha: false });
  renderer.setSize(W(), H());
  renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
  renderer.shadowMap.enabled = true;
  renderer.shadowMap.type = THREE.PCFSoftShadowMap;
  renderer.toneMapping = THREE.ACESFilmicToneMapping;
  renderer.toneMappingExposure = 1.2;
  renderer.setClearColor(0x060402);

  // Scene & Camera
  const scene = new THREE.Scene();
  scene.fog = new THREE.FogExp2(0x060402, 0.038);

  const camera = new THREE.PerspectiveCamera(42, W()/H(), 0.1, 200);
  camera.position.set(0, 3.5, 12);
  camera.lookAt(0, 0, 0);

  // ── FLOOR ──────────────────────────────────────────
  const floorGeo = new THREE.PlaneGeometry(40, 40, 40, 40);
  const floorMat = new THREE.MeshStandardMaterial({
    color: 0x0a0704,
    roughness: 0.05,
    metalness: 0.92,
    envMapIntensity: 1.4,
  });
  const floor = new THREE.Mesh(floorGeo, floorMat);
  floor.rotation.x = -Math.PI / 2;
  floor.position.y = -2.2;
  floor.receiveShadow = true;
  scene.add(floor);

  // Grid lines on floor
  const gridHelper = new THREE.GridHelper(20, 24, 0xc9a84c, 0xc9a84c);
  gridHelper.position.y = -2.19;
  gridHelper.material.opacity = 0.06;
  gridHelper.material.transparent = true;
  scene.add(gridHelper);

  // ── TABLE ──────────────────────────────────────────
  // Table top (round)
  const tableGeo = new THREE.CylinderGeometry(1.3, 1.3, 0.12, 64);
  const tableMat = new THREE.MeshStandardMaterial({
    color: 0x1a0e06,
    roughness: 0.15,
    metalness: 0.3,
  });
  const table = new THREE.Mesh(tableGeo, tableMat);
  table.position.set(0, -0.8, 0);
  table.castShadow = true;
  table.receiveShadow = true;
  scene.add(table);

  // Table edge ring
  const ringGeo = new THREE.TorusGeometry(1.3, 0.015, 12, 80);
  const ringMat = new THREE.MeshStandardMaterial({ color: 0xc9a84c, roughness: 0.2, metalness: 0.9 });
  const ring = new THREE.Mesh(ringGeo, ringMat);
  ring.position.set(0, -0.73, 0);
  ring.rotation.x = Math.PI / 2;
  scene.add(ring);

  // Table leg
  const legGeo = new THREE.CylinderGeometry(0.06, 0.14, 1.4, 16);
  const legMat = new THREE.MeshStandardMaterial({ color: 0x0f0904, roughness: 0.5, metalness: 0.4 });
  const leg = new THREE.Mesh(legGeo, legMat);
  leg.position.set(0, -1.5, 0);
  leg.castShadow = true;
  scene.add(leg);

  // ── PLATES ─────────────────────────────────────────
  function makePlace(x, z, delay) {
    const g = new THREE.Group();

    // Plate base
    const pg = new THREE.CylinderGeometry(0.32, 0.3, 0.03, 48);
    const pm = new THREE.MeshStandardMaterial({ color: 0xf0e8dc, roughness: 0.3, metalness: 0.05 });
    const p = new THREE.Mesh(pg, pm);
    p.position.y = -0.73;
    p.castShadow = true;
    g.add(p);

    // Gold rim
    const rg = new THREE.TorusGeometry(0.3, 0.008, 8, 48);
    const rm = new THREE.MeshStandardMaterial({ color: 0xc9a84c, roughness: 0.1, metalness: 1 });
    const r = new THREE.Mesh(rg, rm);
    r.position.y = -0.71;
    r.rotation.x = Math.PI / 2;
    g.add(r);

    // Cloche dome
    const dg = new THREE.SphereGeometry(0.2, 32, 16, 0, Math.PI*2, 0, Math.PI/2);
    const dm = new THREE.MeshStandardMaterial({
      color: 0xd8d0c4,
      roughness: 0.05,
      metalness: 0.85,
      transparent: true,
      opacity: 0.6,
    });
    const d = new THREE.Mesh(dg, dm);
    d.position.y = -0.71;
    d.castShadow = true;
    g.add(d);

    g.position.set(x, 0, z);
    scene.add(g);

    // gentle float
    g._delay = delay;
    g._baseY = 0;
    return g;
  }

  const plates = [
    makePlace(-0.6, 0.5, 0),
    makePlace(0.7, -0.3, 1.2),
    makePlace(-0.1, -0.7, 2.4),
  ];

  // ── WINE BOTTLE ────────────────────────────────────
  function makeBottle() {
    const g = new THREE.Group();

    // Body
    const bg = new THREE.CylinderGeometry(0.13, 0.13, 0.7, 24);
    const bm = new THREE.MeshStandardMaterial({
      color: 0x080302, roughness: 0.08, metalness: 0.4,
      transparent: true, opacity: 0.88,
    });
    const b = new THREE.Mesh(bg, bm);
    b.position.y = 0;
    b.castShadow = true;
    g.add(b);

    // Shoulder
    const sg = new THREE.CylinderGeometry(0.08, 0.13, 0.18, 20);
    const s = new THREE.Mesh(sg, bm);
    s.position.y = 0.44;
    g.add(s);

    // Neck
    const ng = new THREE.CylinderGeometry(0.04, 0.08, 0.28, 16);
    const n = new THREE.Mesh(ng, bm);
    n.position.y = 0.69;
    g.add(n);

    // Gold label band
    const lg = new THREE.CylinderGeometry(0.135, 0.135, 0.18, 24);
    const lm = new THREE.MeshStandardMaterial({ color: 0xc9a84c, roughness: 0.2, metalness: 0.9, transparent: true, opacity: 0.7 });
    const l = new THREE.Mesh(lg, lm);
    l.position.y = -0.1;
    g.add(l);

    // Cork top
    const cg = new THREE.CylinderGeometry(0.03, 0.04, 0.06, 12);
    const cm = new THREE.MeshStandardMaterial({ color: 0xf0d8a0, roughness: 0.7 });
    const c = new THREE.Mesh(cg, cm);
    c.position.y = 0.87;
    g.add(c);

    g.position.set(1.05, -0.05, -0.3);
    scene.add(g);
    return g;
  }

  const bottle = makeBottle();

  // ── CANDLES ────────────────────────────────────────
  function makeCandle(x, z) {
    const g = new THREE.Group();

    const cg = new THREE.CylinderGeometry(0.04, 0.04, 0.55, 12);
    const cm = new THREE.MeshStandardMaterial({ color: 0xf5ead2, roughness: 0.8 });
    const c = new THREE.Mesh(cg, cm);
    c.position.y = 0.27;
    g.add(c);

    // flame glow point light
    const light = new THREE.PointLight(0xffd080, 0.9, 3, 2);
    light.position.set(x, 0.62, z);
    light._flickerBase = 0.9;
    scene.add(light);

    g.position.set(x, -0.75, z);
    scene.add(g);
    return { g, light };
  }

  const candles = [
    makeCandle(-0.85, -0.25),
    makeCandle(0.3, 0.85),
  ];

  // ── CHAIRS ─────────────────────────────────────────
  function makeChair(angle, dist) {
    const g = new THREE.Group();

    // Seat
    const sg = new THREE.BoxGeometry(0.7, 0.06, 0.6);
    const sm = new THREE.MeshStandardMaterial({ color: 0x100806, roughness: 0.6, metalness: 0.1 });
    const s = new THREE.Mesh(sg, sm);
    s.position.y = 0;
    g.add(s);

    // Back
    const bg = new THREE.BoxGeometry(0.66, 0.7, 0.05);
    const b = new THREE.Mesh(bg, sm);
    b.position.set(0, 0.38, -0.28);
    g.add(b);

    // Legs
    [[-0.28,-0.25],[-0.28,0.25],[0.28,-0.25],[0.28,0.25]].forEach(([lx,lz]) => {
      const lg2 = new THREE.CylinderGeometry(0.02, 0.02, 0.5, 8);
      const ll = new THREE.Mesh(lg2, sm);
      ll.position.set(lx, -0.28, lz);
      g.add(ll);
    });

    // Gold accent on back
    const ag = new THREE.BoxGeometry(0.5, 0.02, 0.02);
    const am = new THREE.MeshStandardMaterial({ color: 0xc9a84c, roughness: 0.1, metalness: 0.9 });
    const a = new THREE.Mesh(ag, am);
    a.position.set(0, 0.55, -0.27);
    g.add(a);

    g.position.set(
      Math.cos(angle) * dist,
      -1.15,
      Math.sin(angle) * dist
    );
    g.rotation.y = angle + Math.PI;
    g.castShadow = true;
    scene.add(g);
    return g;
  }

  makeChair(0, 2.0);
  makeChair(Math.PI * 0.65, 1.95);
  makeChair(Math.PI * 1.35, 2.0);

  // ── WINE GLASSES ───────────────────────────────────
  function makeGlass(x, z) {
    const g = new THREE.Group();
    const mat = new THREE.MeshStandardMaterial({
      color: 0xd0c8bc, roughness: 0.03, metalness: 0.0,
      transparent: true, opacity: 0.22,
    });

    // Bowl (sphere slice)
    const bwl = new THREE.SphereGeometry(0.14, 20, 20, 0, Math.PI*2, 0, Math.PI*0.55);
    const bowl = new THREE.Mesh(bwl, mat);
    bowl.position.y = 0.28;
    bowl.rotation.x = Math.PI;
    g.add(bowl);

    // Stem
    const stG = new THREE.CylinderGeometry(0.008, 0.008, 0.28, 8);
    const stem = new THREE.Mesh(stG, mat);
    stem.position.y = 0.09;
    g.add(stem);

    // Base
    const bsG = new THREE.CylinderGeometry(0.1, 0.09, 0.02, 20);
    const base = new THREE.Mesh(bsG, mat);
    base.position.y = -0.04;
    g.add(base);

    g.position.set(x, -0.72, z);
    scene.add(g);
    return g;
  }
  makeGlass(-0.3, 0.2);
  makeGlass(0.3, -0.5);

  // ── FLOATING PARTICLES ─────────────────────────────
  const particleCount = 200;
  const positions = new Float32Array(particleCount * 3);
  const particleSizes = new Float32Array(particleCount);
  for (let i = 0; i < particleCount; i++) {
    positions[i*3]   = (Math.random() - .5) * 18;
    positions[i*3+1] = Math.random() * 10 - 2;
    positions[i*3+2] = (Math.random() - .5) * 14;
    particleSizes[i] = Math.random() * 2.5 + 0.5;
  }
  const partGeo = new THREE.BufferGeometry();
  partGeo.setAttribute('position', new THREE.BufferAttribute(positions, 3));
  partGeo.setAttribute('size', new THREE.BufferAttribute(particleSizes, 1));
  const partMat = new THREE.PointsMaterial({
    color: 0xc9a84c, size: 0.025,
    transparent: true, opacity: 0.45,
    sizeAttenuation: true,
  });
  const particles = new THREE.Points(partGeo, partMat);
  scene.add(particles);

  // ── LIGHTS ─────────────────────────────────────────
  // Ambient fill
  const ambient = new THREE.AmbientLight(0x120a06, 0.6);
  scene.add(ambient);

  // Key — warm overhead spotlight on table
  const keyLight = new THREE.SpotLight(0xfff0d0, 2.5, 12, Math.PI/7, 0.35, 1.8);
  keyLight.position.set(0, 7, 1);
  keyLight.target.position.set(0, -0.8, 0);
  keyLight.castShadow = true;
  keyLight.shadow.mapSize.set(2048, 2048);
  keyLight.shadow.bias = -0.0005;
  scene.add(keyLight);
  scene.add(keyLight.target);

  // Rim — cool blue from behind
  const rimLight = new THREE.DirectionalLight(0x2040a0, 0.35);
  rimLight.position.set(-5, 4, -8);
  scene.add(rimLight);

  // Gold accent — from side
  const accentLight = new THREE.PointLight(0xc9a84c, 1.2, 9, 2);
  accentLight.position.set(3.5, 2, -1);
  scene.add(accentLight);

  // Warm back fill
  const fillLight = new THREE.PointLight(0xff6020, 0.4, 15, 2);
  fillLight.position.set(-4, 1, -6);
  scene.add(fillLight);

  // ── MOUSE PARALLAX ─────────────────────────────────
  let mouseX = 0, mouseY = 0;
  let targetX = 0, targetY = 0;
  document.addEventListener('mousemove', e => {
    mouseX = (e.clientX / W() - .5) * 2;
    mouseY = (e.clientY / H() - .5) * 2;
  });

  // ── SCROLL HIDE ────────────────────────────────────
  // Stop animating if scrolled away
  let heroVisible = true;
  const heroEl = document.getElementById('hero');

  // ── CLOCK ──────────────────────────────────────────
  const clock = new THREE.Clock();

  // ── ANIMATE ────────────────────────────────────────
  function animate() {
    requestAnimationFrame(animate);

    const t = clock.getElapsedTime();

    if (!heroVisible) { return; }

    // Camera parallax
    targetX += (mouseX * 1.4 - targetX) * 0.04;
    targetY += (-mouseY * 0.7 - targetY) * 0.04;
    camera.position.x = targetX;
    camera.position.y = 3.5 + targetY * 0.5;
    camera.lookAt(0, -0.2, 0);

    // Bottle gentle rotation
    bottle.rotation.y = t * 0.18 + Math.sin(t * 0.5) * 0.12;
    bottle.position.y = -0.05 + Math.sin(t * 0.6) * 0.06;

    // Plates float
    plates.forEach((p, i) => {
      p.position.y = Math.sin(t * 0.5 + p._delay) * 0.035;
    });

    // Candle flicker
    candles.forEach((c, i) => {
      c.light.intensity = c.light._flickerBase + Math.sin(t * 18 + i * 5) * 0.08 + Math.random() * 0.05;
    });

    // Particles drift
    const pos = partGeo.attributes.position.array;
    for (let i = 0; i < particleCount; i++) {
      pos[i*3+1] += 0.003;
      if (pos[i*3+1] > 8) pos[i*3+1] = -2;
      pos[i*3] += Math.sin(t * 0.3 + i) * 0.0008;
    }
    partGeo.attributes.position.needsUpdate = true;

    // Key light slow sway
    keyLight.position.x = Math.sin(t * 0.22) * 0.8;

    renderer.render(scene, camera);
  }

  animate();

  // Intersection observer — only render when hero visible
  const io = new IntersectionObserver(entries => {
    heroVisible = entries[0].isIntersecting;
  }, { threshold: 0.01 });
  io.observe(heroEl);

  // Resize
  window.addEventListener('resize', () => {
    camera.aspect = W() / H();
    camera.updateProjectionMatrix();
    renderer.setSize(W(), H());
  });
})();

/* ══════════════════════════════════════════════
   UI INTERACTIONS
   ══════════════════════════════════════════════ */

// ── CURSOR ─────────────────────────────────────────
(function() {
  const cur = document.getElementById('cursor');
  const ring = document.getElementById('cursor-ring');
  let rx = 0, ry = 0, cx = 0, cy = 0;

  document.addEventListener('mousemove', e => {
    cx = e.clientX; cy = e.clientY;
    cur.style.left = cx + 'px';
    cur.style.top = cy + 'px';
  });

  function animRing() {
    rx += (cx - rx) * 0.14;
    ry += (cy - ry) * 0.14;
    ring.style.left = rx + 'px';
    ring.style.top = ry + 'px';
    requestAnimationFrame(animRing);
  }
  animRing();
})();

// ── HERO SPOTLIGHT ─────────────────────────────────
(function() {
  const spot = document.getElementById('hero-spot');
  document.getElementById('hero').addEventListener('mousemove', e => {
    const x = (e.clientX / window.innerWidth * 100).toFixed(1);
    const y = (e.clientY / window.innerHeight * 100).toFixed(1);
    spot.style.background = `radial-gradient(circle 380px at ${x}% ${y}%,rgba(201,168,76,.08),transparent 65%)`;
  });
})();

// ── NAV SCROLL ─────────────────────────────────────
(function() {
  const nav = document.getElementById('nav');
  window.addEventListener('scroll', () => {
    nav.classList.toggle('scrolled', window.scrollY > 60);
  }, { passive: true });
})();

// ── SCROLL REVEAL ──────────────────────────────────
(function() {
  const io = new IntersectionObserver((entries) => {
    entries.forEach(e => {
      if (e.isIntersecting) {
        e.target.classList.add('visible');
        io.unobserve(e.target);
      }
    });
  }, { threshold: 0.12, rootMargin: '0px 0px -60px 0px' });

  document.querySelectorAll('.reveal').forEach(el => io.observe(el));
})();

// ── MENU DRAG SCROLL ───────────────────────────────
(function() {
  const track = document.getElementById('menuTrack');
  let isDown = false, startX, scrollLeft;
  track.addEventListener('mousedown', e => { isDown = true; startX = e.pageX - track.offsetLeft; scrollLeft = track.scrollLeft; });
  track.addEventListener('mouseleave', () => isDown = false);
  track.addEventListener('mouseup', () => isDown = false);
  track.addEventListener('mousemove', e => {
    if (!isDown) return;
    e.preventDefault();
    const x = e.pageX - track.offsetLeft;
    track.scrollLeft = scrollLeft - (x - startX) * 1.2;
  });
})();
</script>
</body>
</html>
